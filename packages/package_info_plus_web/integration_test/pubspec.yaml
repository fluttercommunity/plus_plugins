name: regular_integration_tests
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  package_info_plus_web:
    path: ../
  http: ^0.13.0
  mockito: ^5.0.0
  integration_test:
    sdk: flutter

dependency_overrides:
  args: ^2.0.0
  convert: ^3.0.0
  crypto: ^3.0.0
